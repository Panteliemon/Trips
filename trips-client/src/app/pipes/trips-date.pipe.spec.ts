import { TripsDatePipe } from './trips-date.pipe';

describe('TripsDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TripsDatePipe();
    expect(pipe).toBeTruthy();
  });
});
