import { TripsDateTimePipe } from './trips-date-time.pipe';

describe('TripsDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TripsDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
