import { AdaptiveDatePipe } from './adaptive-date.pipe';

describe('AdaptiveDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AdaptiveDatePipe();
    expect(pipe).toBeTruthy();
  });
});
